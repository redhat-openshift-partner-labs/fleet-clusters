# Archive

Preserved cluster specs after successful deprovision. Each subdirectory was moved here automatically by the post-deprovision GitHub Action:

```
git mv provision/<cluster-id> archive/<cluster-id>
```

These directories are retained for audit and reference. They are not monitored by the EventListener and will not trigger any pipelines.
